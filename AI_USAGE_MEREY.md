# AI Assistance & Usage Log

**Developer:** Merey Kuatbay  
**AI Model Used:** Claude 3.5 Sonnet (Anthropic)  
**Academic Honesty Statement:** Claude was employed as a technical reference and workflow assistant for syntax validation, Git recovery, accessibility auditing, and form architecture. All code output was manually implemented, evaluated against course constraints (pure HTML5, zero external styling), and validated through the W3C Nu HTML Validator.

---

## Log of Interactions

| Date | Intent & User Prompt | Claude Output & Recommended Solution | Critical Evaluation & Student Implementation |
| :--- | :--- | :--- | :--- |
| **2026-09-08** | Establishing root document metadata and character encoding best practices. | Claude recommended explicit `<meta charset="UTF-8">`, responsive `<meta name="viewport">`, descriptive `<meta name="description">`, and proper document outline titles. | Configured head blocks across `index.html`, `menu.html`, and `order.html` to guarantee search indexing standards and encoding accuracy. |
| **2026-09-09** | Validating table structures and resolving W3C warning regarding `border="1"` on `<table>`. | Claude explained that presentation attributes are obsolete in HTML5 and suggested stripping `border="1"` entirely, keeping standard semantic `<table>` tags. | Verified standard HTML5 specification; removed obsolete `border` attribute from `menu.html` and confirmed clean green pass on W3C validator. |
| **2026-09-10** | Ensuring persistent cross-page navigation links without broken relative paths. | Claude advised using root-relative or flat relative links (`index.html`, `menu.html`) wrapped in `<nav><ul><li>` to preserve accessibility tree structure. | Verified navigation integrity across all author pages, ensuring circular navigation without broken hyperlinks. |
| **2026-09-11** | Structuring an accessible pre-order form with item grouping, input validation, and notes. | Claude suggested organizing fields into logical `<fieldset>` containers with `<legend>`, using `<optgroup>` inside select elements, and using `<textarea>` for kitchen notes. | Implemented the suggested grouping in `order.html`, ensuring all `<input>` elements had associated explicit `<label for="...">` bindings and client-side constraints (`required`, `type="tel"`). |
| **2026-09-11** | Resolving terminal lock inside Vim editor during unexpected Git merge commit prompt. | Claude provided navigation commands for Vim (`Esc`, `:wq`, `Enter`) and explained how to bypass editor prompts using `git commit --no-edit`. | Followed Vim exit commands directly in PowerShell terminal, successfully completing the merge commit of upstream files. |
| **2026-09-11** | Re-linking remote repository after accidental deletion of `.git` folder during directory restructuring. | Claude provided sequence to re-initialize Git: `git init`, `remote add origin`, `fetch origin main`, and `reset --mixed origin/main`. | Successfully executed command sequence, recovering tracking of existing repository without overwriting local validation screenshots. |
| **2026-09-12** | Validating accessibility compliance for tabular data in the beverage pricing catalog. | Claude emphasized using `<th scope="col">` for column headers and `<th scope="row">` for individual menu items rather than standard `<td>` data cells. | Refactored `menu.html` rows to use proper table header scopes, improving screen reader comprehension for visually impaired users. |
| **2026-09-16** | Transitioning project theme to Kent Sandwich (Adolf Yanushkevich 1) and implementing full semantic HTML5 requirements across `index.html`, `menu.html`, and `order.html`. | AI assisted with checking required HTML5 tags checklist, ensuring WCAG accessibility compliance (explicit `label[for]` to `input[id]` mapping, `th[scope]`), and designing a complete takeaway form including `date`, `number`, `tel`, and `reset` controls without server backend. | Reviewed all recommendations against course rubric: eliminated any CSS/JS traces, verified 100% semantic skeleton tags (`header`, `nav`, `main`, `footer`), added explanatory rationale comments, and confirmed zero HTML validation errors. |
