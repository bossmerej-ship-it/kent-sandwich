# AI Assistance & Usage Log

**Developer:** Timur
**AI Model Used:** Claude (Anthropic)
**Assignment:** Introduction to Web Technologies — Assignment 3 (Bootstrap: grid system, containers, responsive behavior, typography, buttons, utilities, one Bootstrap component)
**Academic Honesty Statement:** Per the course AI policy, Claude was used only to explain Bootstrap concepts and clarify how specific classes/components work. Claude did not rebuild any pages, write markup, or produce the component/utility code directly. All HTML, class choices, and comments were authored independently and adapted from the official Bootstrap documentation where required.

## Log of Interactions

| Date | Student Question | Claude's Explanation | How It Was Used |
|---|---|---|---|
| 2026-09-19 | What's actually the difference between `container` and `container-fluid`? | Explained that `container` uses fixed, responsive max-widths at each breakpoint while `container-fluid` is always 100% width of the viewport with no max-width cap. | Used to decide and justify in a code comment where each was applied (fixed container for the main content, fluid for the full-width hero/banner section). |
| 2026-09-19 | How do Bootstrap's grid breakpoints (`col-12 col-md-6 col-lg-4`) actually resolve at different widths? | Broke down that each `col-*` class applies from that breakpoint upward until overridden by a larger one, so the smallest class listed is the mobile-first default. | Used to correctly stack the class order on the cards/services row and confirm the intended 1 → 2 → 3 column behavior across phone, tablet, and desktop. |
| 2026-09-20 | What do the gutter classes (`g-*`, `gx-*`, `gy-*`) actually control, and how are they different from just adding margin? | Explained that gutter classes adjust the built-in padding/negative-margin spacing between columns inside a row consistently, without breaking the grid's alignment the way manual margins can. | Applied gutter classes instead of custom margin on the row of cards, removing the leftover manual spacing from Assignment 2. |
| 2026-09-20 | How does nesting a row inside a column work, and why would you need it? | Explained that a nested `row` inside a `col-*` creates its own independent 12-column grid scoped to that column's width, useful for splitting a card's content further. | Used to nest a two-column row (icon + text) inside one of the main columns on the services section. |
| 2026-09-21 | What's the difference between the responsive display utilities like `d-none d-md-block`? | Explained that `d-none` hides an element by default and the paired breakpoint class (e.g. `d-md-block`) re-enables display starting at that breakpoint, following the same mobile-first cascade as the grid. | Used to hide a secondary nav element on phone width and show it again from tablet width up. |
| 2026-09-21 | Why does Bootstrap's navbar need the JS bundle for the toggler to work, since it's "just CSS"? | Explained that the collapse/toggle behavior (`data-bs-toggle="collapse"`) depends on Bootstrap's JavaScript component to add/remove the `show` class on click; without the bundle the button renders but does nothing. | Confirmed the JS bundle link was needed on every page for the collapsible navbar to actually function on small screens. |
| 2026-09-22 | Which of my old Assignment 2 CSS rules are now redundant once Bootstrap's grid and spacing utilities are in place? | Went through the categories generally (hand-written column/float rules, manual margin/padding for layout, custom button styling) and explained why each is superseded by Bootstrap's grid and utility classes. | Used as a checklist while manually reviewing and deleting the old rules from `base.css`, then wrote up the removed-rules list required by the assignment. |
